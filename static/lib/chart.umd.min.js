// Chart.js v4.4.7 UMD Build — MIT License
// Download from: https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.js
// Paste the minified content below this comment, replacing this placeholder.
// For now, this is a stub that creates a minimal Chart constructor.
(function(global){
    if (global.Chart) return;
    global.Chart = function(ctx, config) {
        this.ctx = ctx;
        this.config = config;
        this.data = config.data || {};
        this.options = config.options || {};
        console.warn('Chart.js stub: replace static/lib/chart.umd.min.js with the real library');
    };
    global.Chart.prototype.destroy = function() {};
    global.Chart.prototype.update = function() {};
    global.Chart.prototype.resize = function() {};
    global.Chart.prototype.toBase64Image = function() { return ''; };
    global.Chart.register = function() {};
})(typeof window !== 'undefined' ? window : this);
